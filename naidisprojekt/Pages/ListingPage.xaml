<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="naidisprojekt.Pages.ListingPage"
             xmlns:converters="clr-namespace:naidisprojekt.Converters"
             Title="ListingPage"
             >
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ByteToImageConverter x:Key="ByteToImageConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <VerticalStackLayout>
        <Grid RowDefinitions="0.5*,0.4*, 0.1*">
            <Image Grid.Row="0" Source="{Binding ListingImage, Converter={StaticResource ByteToImageConverter}}"
               Aspect="AspectFill"/>

            <VerticalStackLayout Grid.Row="1" BackgroundColor="White" Padding="20">
                <Label Text="{Binding ListingName}" FontSize="22" FontAttributes="Bold"/>
                <Label Text="{Binding Price, StringFormat='${0:F2}'}" FontSize="18" TextColor="DarkGreen"/>
                <Label Text="{Binding ListingDescription}" FontSize="14" TextColor="Gray"/>
            </VerticalStackLayout>
          
        </Grid>
    </VerticalStackLayout>
</ContentPage>